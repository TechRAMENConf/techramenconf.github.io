---
import personalSponsors from "../data/personal_sponsors.json";
import PersonalSponsorImage from "./PersonalSponsorImage.astro";
---

<section class="section-colored section-accent" id="personal-sponsors">
  <div class="container">
    <h2 class="handwritten-title text-center text-accent mb-12 led-text">
      ラーメン応援団
    </h2>
    
    <div class="card p-8 md:p-12 max-w-4xl mx-auto glow-accent">
      <div class="flex flex-wrap justify-center gap-4">
        {
          personalSponsors.map((sponsor) => (
            <div class="relative group">
              {sponsor.twitterUrl ? (
                <a href={sponsor.twitterUrl} class="block transition-transform hover:scale-110 glow-primary">
                  <PersonalSponsorImage avatarUrl={sponsor.avatarUrl} />
                </a>
              ) : (
                <div class="transition-transform hover:scale-110 glow-primary">
                  <PersonalSponsorImage avatarUrl={sponsor.avatarUrl} />
                </div>
              )}
              <div class="absolute z-10 p-2 -mt-2 text-sm text-primary bg-panel rounded-lg opacity-0 transition-opacity duration-300 group-hover:opacity-100 left-1/2 transform -translate-x-1/2 top-full border border-primary/30 led-text">
                {sponsor.userName}
              </div>
            </div>
          ))
        }
      </div>
      
      <div class="mt-12 text-center">
        <p class="text-accent/80 mb-6 font-kikai">
          個人スポンサーとして応援していただける方を募集しています
        </p>
        <a href="https://note.com/tomio2480/n/n9280fae6ab29?magazine_key=m8fb20b0824ea" class="btn btn-primary led-text">
          スポンサーになる
        </a>
      </div>
    </div>
  </div>
</section>
